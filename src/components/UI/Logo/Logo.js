import React from 'react';
import classes from './Logo.css';

import img from '../../../assets/img/logo.png';

const logo = (props)=>(
  <h1 className={classes.Logo}>Experts</h1>
);
export default logo;